import { DashBoardType } from "@/types";
import { CalendarCheckIcon } from "lucide-react";

const dashBoardEvent: DashBoardType = {
  title: "Events",
  slug: "/admin/events",
  icon: CalendarCheckIcon,
};

export default dashBoardEvent;
